// // Seleccionar

// console.log(target)
// // Crear una nueva instancia del Intersection Observer
// const observer = new IntersectionObserver((entries) => {
//     // entries es un arreglo de objetos que describen los cambios en la intersección
//     entries.forEach(entry => {
//         // Verificar si el elemento objetivo está dentro del área de intersección
//         if (entry.isIntersecting) {
//             console.log('El elemento está dentro del área de intersección');
//         } else {
//             console.log('El elemento está fuera del área de intersección');
//         }
//     });
// }, { threshold: 0.5 });

// // Comenzar a observar el elemento
// observer.observe(target);
